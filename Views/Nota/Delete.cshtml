@model ProjetoGame.Models.Nota

@{
    ViewData["Title"] = "Delete";
}

<h1>Deletar</h1>

<h3>Tem certeza de que deseja excluir isto?</h3>
<div>
    <h4>Nota</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.NotaValor)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.NotaValor)
        </dd>
    </dl>
    
    <form asp-action="Deletar">
        <input type="hidden" asp-for="Id" />
        <input type="submit" value="Deletar" class="botao" /> |
        <a asp-action="Index">Voltar</a>
    </form>
</div>
